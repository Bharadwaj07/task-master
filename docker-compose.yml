services:
  mongodb:
    image: mongo:7.0
    container_name: task-master-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=taskmaster
    restart: unless-stopped

volumes:
  mongodb_data:
